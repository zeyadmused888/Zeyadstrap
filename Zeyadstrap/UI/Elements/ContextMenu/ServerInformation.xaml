<base:WpfUiWindow x:Class="Zeyadstrap.UI.Elements.ContextMenu.ServerInformation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zeyadstrap.UI.Elements.ContextMenu"
        xmlns:base="clr-namespace:Zeyadstrap.UI.Elements.Base"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:models="clr-namespace:Zeyadstrap.UI.ViewModels.ContextMenu"
        xmlns:resources="clr-namespace:Zeyadstrap.Resources"
        d:DataContext="{d:DesignInstance Type=models:ServerInformationViewModel}"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.ContextMenu_ServerInformation_Title}"
        MinWidth="0"
        MinHeight="0"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" x:Name="RootTitleBar" Title="{x:Static resources:Strings.ContextMenu_ServerInformation_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" Icon="pack://application:,,,/Zeyadstrap.ico" />

        <Border Grid.Row="1" Margin="16,10,16,16" Padding="16" CornerRadius="6" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="132" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,16,12" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_ServerInformation_Type}" FontWeight="Medium" />
                <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ServerType, Mode=OneWay}" TextWrapping="Wrap" />

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,16,12" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_ServerInformation_InstanceId}" FontWeight="Medium" />
                <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding InstanceId, Mode=OneWay}" TextWrapping="Wrap" />

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,16,0" VerticalAlignment="Center" Text="{x:Static resources:Strings.ContextMenu_ServerInformation_Location}" FontWeight="Medium" Visibility="{Binding ServerLocationVisibility, Mode=OneTime}" />
                <TextBlock Grid.Row="2" Grid.Column="1" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{Binding ServerLocation, Mode=OneWay}" TextWrapping="Wrap" Visibility="{Binding ServerLocationVisibility, Mode=OneTime}" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Padding="16,12" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Right">
                <ui:Button MinWidth="120" Icon="Copy24" Content="{x:Static resources:Strings.ContextMenu_ServerInformation_CopyInstanceId}" Command="{Binding CopyInstanceIdCommand, Mode=OneTime}" />
                <ui:Button Margin="10,0,0,0" MinWidth="104" Content="{x:Static resources:Strings.Common_Close}" IsCancel="True" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>

<base:WpfUiWindow x:Class="Zeyadstrap.UI.Elements.Dialogs.FluentMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Zeyadstrap.UI.Elements.Dialogs"
        xmlns:base="clr-namespace:Zeyadstrap.UI.Elements.Base"
        xmlns:controls="clr-namespace:Zeyadstrap.UI.Elements.Controls"
        mc:Ignorable="d"
        Title="Zeyadstrap"
        d:DesignWidth="520"
        MinWidth="320"
        MaxWidth="540"
        Width="420"
        MinHeight="180"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" x:Name="RootTitleBar" Title="Zeyadstrap" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" Icon="pack://application:,,,/Zeyadstrap.ico" />

        <Border Grid.Row="1" Margin="16,10,16,16" Padding="16" CornerRadius="6" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image x:Name="IconImage" Grid.Column="0" RenderOptions.BitmapScalingMode="HighQuality" Width="36" Height="36" Margin="0,2,16,0" VerticalAlignment="Top" />
                <controls:MarkdownTextBlock x:Name="MessageTextBlock" Grid.Column="1" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Padding="16,12" Background="{ui:ThemeResource SolidBackgroundFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight" HorizontalAlignment="Right">
                <Button x:Name="ButtonOne" MinWidth="104" Content="Button 1" />
                <Button x:Name="ButtonTwo" MinWidth="104" Margin="10,0,0,0" Content="Button 2" />
                <Button x:Name="ButtonThree" MinWidth="104" Margin="10,0,0,0" Content="Button 3" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>
